import { Box } from "@mui/material";

export const SignalPanel = () => {
  return (
    <Box
      sx={{
        display: "flex",
        justifyContent: "center",
        height: 360,
      }}
    ></Box>
  );
};
